# AI Gateway configuration
PORT=8000
LOG_LEVEL=info

# Embeddings / LLM providers (fill in if using external APIs)
OPENAI_API_KEY=
EMBEDDING_MODEL=text-embedding-3-large
LLM_MODEL=gpt-5

# Vector DB connection (if used later)
VECTOR_DB_URL=
VECTOR_DB_API_KEY=
