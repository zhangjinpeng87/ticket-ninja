from typing import Dict, Optional

# Placeholder for OCR + ViT features

def parse_screenshot(screenshot_id: str) -> Optional[Dict[str, str]]:
    # In real impl, fetch binary by id, run OCR + feature extraction
    return {
        "error_summary": "[Screenshot] Detected error: TimeoutError at /api/payments",
    }
